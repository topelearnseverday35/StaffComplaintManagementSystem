package FinalYearProject.StaffComplaintMgmtSystem.enums;

public enum Status {

        OPEN,           // Just submitted
        IN_PROGRESS,      // Admin is looking at it
        RESOLVED,       // Admin resolved it
        CLOSED,         // Closed (resolved or dismissed)
        REJECTED        // Admin rejected it
    }

